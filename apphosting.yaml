# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
# Forzando reconstrucción para resolver problemas de despliegue.
# Invalidando la caché de nuevo para asegurar una compilación limpia.
# Comentario final para forzar la reconstrucción definitiva.
# Invalidando la caché por última vez para corregir el error de react-day-picker.
# Forzando reconstrucción para resolver ChunkLoadError y asegurar la estabilidad.
# Forzando reconstrucción para resolver un nuevo ChunkLoadError.
# Forzando reconstrucción para intentar solucionar el error de carga de chunks (ChunkLoadError).
# Forzando la reconstrucción para resolver un nuevo error de carga de chunks (ChunkLoadError).
# Forzando reconstrucción para resolver el último ChunkLoadError.
# Forzando reconstrucción de nuevo para resolver ChunkLoadError.
# Forzando la reconstrucción para corregir el error de carga de chunks de la página de rutas.
# Forzando reconstrucción una vez más para resolver el ChunkLoadError.
# Forzando una nueva reconstrucción para resolver el error ChunkLoadError.